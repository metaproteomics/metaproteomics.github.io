---
/**
 * Container
 * A layout wrapper that centers content with max-width constraint.
 * Usage: Wraps page sections to ensure consistent horizontal spacing and centering.
 */
const { class: className } = Astro.props;
---

<div class:list={["max-w-screen-xl mx-auto px-5", className]}>
  <slot />
</div>
